html
    head
        meta(charset='utf-8')
        title 메인화면
    body
        h1 로그인 상태 : #{authInfo.loginStatus}
        -if(authInfo.loginStatus == true){
            h1 #{authInfo.username} 님 환영합니다
            h1
                a(href='/logout') 로그아웃 하기
        -}
        -else {
            h1 
                a(href='/login') 로그인 하기
            h1 
                a(href='/register') 회원가입 하기
        - }